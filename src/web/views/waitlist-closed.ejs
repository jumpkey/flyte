<div class="container py-4">
  <div class="panel checkout-panel text-center">
    <h1 class="h3 mb-2">Waitlist closed</h1>
    <p class="text-muted">The waitlist for <strong><%= event.name %></strong> isn't open right now.</p>
    <a href="/events/<%= event.eventId %>" class="btn btn-outline-secondary mt-2">Back to event</a>
  </div>
</div>
